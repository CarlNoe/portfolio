import CategoryTitle from "../../common/CategoryTitle";

function AboutMe() {
  return (
    <section id="about" className="mt-32">
      <CategoryTitle index={1} name="About Me" />
    </section>
  );
}

export default AboutMe;
